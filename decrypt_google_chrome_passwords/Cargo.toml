[package]
name = "decrypt_google_chrome_passwords"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
hex = "0.4.3"
regex = "1.10.3"
rusqlite = { version = "0.26.0", features = ["bundled"] }
